# Working constraints

- Never launch a browser, headless or otherwise, to test changes.
- Never copy the site to a scratchpad or build probe/harness files.
- Make the edits, report what changed, and stop. The user verifies visually.
- Never include a Claude-Session URL in commit messages.
- Never override git user.name or user.email with -c flags.
